feat(phase3): campaign engine — targeting + AI generation + Resend sending

- lib/campaigns/audience.ts: resolve recipients from two sources —
  past requesters (rfq_requests + packaging_plans) or directory companies
  (providers by type/category/emirate), de-duplicated, emails only
- /api/admin/campaigns: list + create draft campaigns
- /api/admin/campaigns/audience: preview audience count + sample
- /api/admin/campaigns/generate: AI subject + HTML body via Claude
- /api/admin/campaigns/send: batched Resend send (100/batch, 500 cap),
  {{name}}/{{company}} tokens, test-send, records campaign_sends + counters
- /dashboard/campaigns: audience builder, AI generate, create, test, send
- sidebar entry "الحملات / Campaigns"

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
